<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
                         http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0"
         metadata-complete="true">

    <!--
    <servlet>元素定义一个Servlet组件
    <servlet-name>标签定义Servlet的名称，这个名称在Servlet容器中必须是唯一的，用于在其他地方引用该Servlet。
    <servlet-class>标签定义Servlet类的完整类名，这个类必须继承HttpServlet并重写doGet或doPost等HTTP请求处理方法
    -->
    <servlet>
        <servlet-name>hello</servlet-name>
        <servlet-class>com.zhehao.servlet.HelloServlet</servlet-class>
    </servlet>

    <!--
    <servlet-mapping>元素用于将Servlet与一个URL路径进行映射，即指定了Servlet要处理的URL模式。
    <servlet-name>标签引用之前定义的Servlet的名称
    <url-pattern>标签指定URL的匹配模式，这里将以/hello开头的HTTP请求将交给名为hello的Servlet进行处理
    可以指定多个servlet-mapping的映射，可使用*通配符指定通用映射路径
    -->
    <servlet-mapping>
        <servlet-name>hello</servlet-name>
        <url-pattern>/hello</url-pattern>
    </servlet-mapping>
    <servlet-mapping>
        <servlet-name>hello</servlet-name>
        <url-pattern>/hello2</url-pattern>
    </servlet-mapping>
    <servlet-mapping>
        <servlet-name>hello</servlet-name>
        <url-pattern>/hello3</url-pattern>
    </servlet-mapping>
    <servlet-mapping>
        <servlet-name>hello</servlet-name>
        <url-pattern>/hello/pattern/*</url-pattern>
    </servlet-mapping>

    <!--
    /* 表示默认的映射，优先级低于上面指定路径的映射
    -->
    <servlet>
        <servlet-name>error</servlet-name>
        <servlet-class>com.zhehao.servlet.ErrorServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>error</servlet-name>
        <url-pattern>/*</url-pattern>
    </servlet-mapping>

</web-app>