var score = 80;

if (score >= 60) {
    // console.log 在浏览器的控制台打印变量
    console.log("score is high");
} else if (score >= 20) {
    // alert()函数用于在浏览器中显示一个简单的警告框，其中包含一条消息和一个确认按钮
    alert("score is higher than 20");
} else {
    alert("score is lower than 20");
}